import { Scene } from 'phaser'
import {SCENE_LOADER} from "../constants";
export class LoaderScene extends Scene {

  constructor() {
    super(SCENE_LOADER);
  }

  preload() { }

  create () { }

}
